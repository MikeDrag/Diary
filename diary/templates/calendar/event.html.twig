{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <div id="event-wrapper">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
</div>

{% endblock %}